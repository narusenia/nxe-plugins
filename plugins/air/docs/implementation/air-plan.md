# Air — 実装計画

> 最終更新: 2026-08-31

要件は [`../requirements/REQ-AIR.md`](../requirements/REQ-AIR.md)。
DSP 仕様は [`../specifications/dsp.md`](../specifications/dsp.md)（式・係数・
時定数・耳で詰める定数の一覧）、UI 仕様は
[`../specifications/ui.md`](../specifications/ui.md)（画面・図・必要な部品）。

**単位ごとに「完了条件」を満たしてからコミットする。** 完了条件は「動いた」
ではなく**測れること**で書く。

## 順序の考え方

```text
AIR-1  ノイズ層と WIDTH      ← ゲート。ここが通らないと製品の主張が立たない
AIR-2  倍音層
AIR-3  BLEND / CHARACTER / FOCUS
AIR-4  ラッパとパラメータ     ← ここで音が出る
AIR-5  Follow Engine          ← 耳が使える状態で作る
AIR-6  保護（Excess Guard）
AIR-7  詰め（レート・ブロック・極端値）
AIR-8  UI マクロ層
AIR-9  UI スペクトルの重ね描き
AIR-10 UI 読み値と Advanced
AIR-11 解析の配線
AIR-12 CPU 予算
AIR-13 既定値と耳
```

**`AIR-1` がゲート**（`REQ-AIR-008`）。`WIDTH` 全域でモノ和に櫛形が出ない
ことが崩れたら、`WIDTH` の設計ごと書き直しになり、その上に載っている
「層を独立した対象として配置する」という主張が成立しない。Velour の `VEL-1`、
Sparkleur の `SPK-2` と同じ位置。

**`AIR-4`（ラッパ）が `AIR-5`（Follow）より前。** Velour が `VEL-5` を
`VEL-4` より前に置いたのと同じ判断。**Follow Engine は Air の正体だが、
効いたかの判断が一番耳寄り**（「元音から Air が出ているように感じるか」）。
層が鳴る状態を先に作れば、そこから先の判断が速い。

**共有クレートの新規単位が無い。** Sparkleur が `SPK-1` で `nxe-audio` を
作ったが、Air が要求する既存ブロックは全部そこにある（`REQ-AIR-015`）。
唯一の新規はノイズ生成器で、それは `air-core` に置く。

---

### AIR-1 — ノイズ層と WIDTH（ゲート）✅ **ゲート通過**

`noise.rs`。**`air-core` クレートもここで作る**（`VEL-1` / `SPK-2` と同じ位置）。

ノイズの生成、傾き、粒立ち、そして `WIDTH` の共有モデル。

- **完了条件**:
  - [ ] `WIDTH` 全域で、**モノ和のスペクトルに櫛形の谷が出ない**
        （平坦性 ±1 dB 以内）
  - [ ] `WIDTH` = 0 で L/R の相関が 1.0、`WIDTH` = 1 で 0 付近
  - [ ] `WIDTH` = 1 のモノ和が片チャネルに対し **−3 dB ±0.5 dB**
  - [ ] `WIDTH` を掃引して相関が単調に減る
  - [ ] 44.1 / 48 / 96 / 192 kHz で**可聴帯域のレベルが ±0.5 dB 以内**
  - [ ] 明示シードで出力が完全に再現する
  - [ ] ゲイン 0 でちょうど無音、**無音入力でキープアライブが厳密に 0**
        （`dsp.md`。Follow の深さ側に下限を置くと `REQ-AIR-007` が壊れるので、
        下限はノイズ層自身が持つ）
  - [ ] 傾きを振り切っても可聴帯域外にエネルギーが逃げない
  - [ ] hostile な値で非有限を出さない
- **要件**: REQ-AIR-004, REQ-AIR-008, REQ-AIR-017
- **依存**: なし
- **決めた**（`dsp.md`）: 傾きは **1 次のティルト 1 段**（極 1 個の低域通過と
  その補数を逆向きのゲインで足す）。ピボットは HPF のコーナーに乗せる。
  `FOCUS` は `2^(focus·1.5)`、`BASE_CORNER_HZ` = 3 kHz
- **決めること**: `TILT_MAX_DB` と `GRAIN_SECONDS` の実値（耳）
- **見込んでいる罠**: **サンプルレート補正を忘れる。** 総パワーは
  レート不変だが `fs/2` に広がるので、可聴帯域の密度がレートに反比例する。
  `sqrt(fs / 48000)` の補正が要る（`REQ-AIR-017`）。**ノイズなので耳では
  気付けない** — 最初からテストで固定する
- **見込んでいる罠**: **テスト用ノイズに DC が乗る。** `SPK-18` で
  `(state >> 8) / 2^23 * 2.0 - 1.0` が値域 `-1..3` になっていて、ピンク化の
  1 段目の極が DC を 425 倍していた。**`nxe_audio::harmonics` の
  `noise` / `pink` は修正済み**なので、そちらを使う。自分で書かない
- **見込んでいる罠**: 相関の測定は `nxe_dsp::Correlation` を使う。
  自前で書くと `1e30²` が f32 で `inf` になり `inf/inf` = NaN になる
  （Doubler で踏んで `CEILING` を入れた）
- **踏んだ罠**: **「あり得ない設定」の番兵は全部のフィールドを外す。**
  `set` はフィールドごとに「動いていなければ何もしない」で守っているので、
  1 つだけ NaN にした番兵だと**残りのフィールドの係数が初期化されない**。
  `width` が既定と一致していて `shared` / `independent` が 0 と 0 のまま
  だったので、**層が完全な無音**で出てきた。テストは 8 本落ちたが、
  落ち方は「ノイズが出ない」ではなく「相関が測れない」だった
- **踏んだ罠**: **櫛形の対照実験に遅延を使うと通ってしまう。** 12 サンプルの
  遅延は 4 kHz ごとに櫛が立ち、それは 1/3 オクターブ帯より細かいので
  帯の裾から隣の山が入り、**0.9 dB しか出ない**。オールパス（`REQ-AIR-008` が
  拒んでいる手法そのもの）に替えて 6.6 dB
- **踏んだ罠**: **レート非依存を広い帯域通過で測ると、測定側が歪む。**
  14 kHz の 2 次ローパスは 44.1 kHz でナイキストの 0.64、192 kHz で 0.15 に
  居るので、同じ「14 kHz」が 10 kHz を 0.4 dB 落とすか 1.0 dB 落とすかが
  変わる。0.53 dB のずれが出て**全部が測定の側だった**。1/3 オクターブ帯で測る
- **測って捨てた**: 上の 1 オクターブのレート差（16 kHz で 3.83 dB）を
  スカラで補正すると、層全体の RMS は 0.72 → 0.30 dB になる代わりに**核の
  4 帯域が 0.39 → 1.26 dB に開く**。平らな誤差を傾きに両替しただけなので
  戻した。詳細と実測値は `dsp.md`

---

### AIR-2 — 倍音層 ✅

`harmonic.rs`。`nxe_audio::shaper` を借りて、前後に HPF、入力に上蓋。

- **完了条件**:
  - [ ] ゲイン 0 でちょうど無音
  - [ ] 4x で最悪ケースの折り返しが **−60 dB 以下**
  - [ ] 上蓋が**ホストレート**に対して効いている（96 / 192 kHz で確認）
  - [ ] `WIDTH` によらず L == R（倍音層は広がらない）
  - [ ] `CHARACTER` で偶奇比が動き、足す量は ±3 dB 以内
- **要件**: REQ-AIR-003
- **依存**: AIR-1
- **決めた**（`dsp.md`）: 生成後 HPF は前段の **1 オクターブ上**（`2·corner`）。
  足すものを「原音の複製」ではなく「原音に無かった倍音」に寄せるため。
  Velour の AIR 帯域は逆に**下へ**ずらしていて、あちらは「その帯域の存在感を
  作る」製品なのでそれが正しい
- **決めること**: `shaper` の `k` = 4.0 で艶が足りるか（耳）
- **見込んでいる罠**: **上蓋を内部レートに対して書く。** Velour が
  `VEL-3` でやった。4x のとき 48 kHz まで開いてしまう。ホストレートの
  1/4（`AIR_INPUT_CEILING`）
- **踏んだ罠**: **`CHARACTER` の効きを「3 倍音 ÷ 2 倍音」で測ると 1.7 % しか
  動かない。** 膝が奇数次を増やすのと同時にバイアスが偶数次も増やすので比が
  相殺される。**「3 倍音 ÷ 基音」で見ると 0.150 → 0.248**（Velour の
  `dsp.md` が最初からそう書いている）。最初のテストは「`CHARACTER` は何も
  しない」と報告したが、層は変わっていた
- **測った**: 折り返し **−70.0 dB**（許容 −60、10 dB 余っている）、
  `CHARACTER` のレベルの動き **0.55 dB**、3.5 kHz の音が層に戻る量
  **−23.7 dB**

---

### AIR-3 — BLEND / CHARACTER / FOCUS ✅

`layer.rs`。2 系統を 1 個の対象としてまとめる。

- **完了条件**:
  - [ ] `BLEND` を掃引して生成層の総エネルギーが ±1 dB 以内で平坦
        （電力保存のクロスフェード）
  - [ ] `BLEND` を振り切ると、もう一方がちょうど無音
  - [ ] `CHARACTER` をどこで動かしても、`BLEND` の位置によらず音が変わる
  - [ ] `FOCUS` で 2 系統の帯域がまとめて動き、比が保たれる
  - [ ] `FOCUS` を振り切っても最上端がナイキストを超えない
  - [ ] 係数の再計算がブロック単位（再構築カウンタで測る）
- **要件**: REQ-AIR-005, REQ-AIR-006
- **依存**: AIR-1, AIR-2
- **決めた**（`dsp.md`）: `CHARACTER` は**常に両系統へ同じ正規化値を渡す**。
  聞こえるのは `BLEND` が選んだ系統だけなので、意味は自動的に移る。分岐は要らない
- **測って決めた**: `NOISE_TRIM` = **0.0359**（−28.90 dB）、`HARMONIC_TRIM` =
  1.0。`BLEND` の掃引が **0.02 dB** で平ら（入れる前は 28.9 dB 動いていた）
- **踏んだ罠**: **広帯域素材では `CHARACTER` の効きが小さく出る。** 層の差分で
  測ると `BLEND` 0 で −23.8 dB しかない。ノイズの倍音はノイズなので、膝を
  動かしてもスペクトルの形が動かないため。**軸が効いている証拠は正弦で測る**
  （`harmonic.rs` の H3/H1）

---

### AIR-4 — ラッパとパラメータ（ここで音が出る）✅

`air` クレート。パラメータ宣言、`process()`、`MIX`。**UI はまだ持たず、
ホストの既定パラメータ UI で音を確認する。**

- **完了条件**:
  - [ ] `MIX` = 0 で出力が入力と**ビット一致**
  - [ ] `SURFACE` = 0 で出力が入力と**ビット一致**
  - [ ] `2→2` と `1→2` の両方のレイアウトで動く
  - [ ] 4 ホストで CLAP と VST3 が読み込まれる
  - [ ] `mise run bundle air` / `install air` が通る
- **要件**: REQ-AIR-001, REQ-AIR-011, REQ-AIR-012, REQ-AIR-014
- **依存**: AIR-3
- **決めた**: **nih-plug のまま。** 移行の範囲が調べたより広い —
  ワークスペースの `vizia` は `nih_plug_vizia` が引くフォークと**バイト単位で
  一致していないとコンパイルが通らない**ので、Air だけ vizia-plug に載せると
  `nxe-ui` と gallery と既存 3 本の UI が全部巻き添えになる。
  「駄目なら戻すのはラッパだけ」が成り立つのは `air-core` まで（`LICENSING.md`）
- **決めた**: `FOLLOW` と偏差はここでは宣言しない。**何もしないコントロールは
  無いコントロールより悪い。** `AIR-5` / `AIR-6` で足す（nih-plug は id で
  引くので後から足すのは安全、**変えるのは安全でない**）
- **決めること**: `CLAP_ID` は `com.nxe.air`。**出荷後に変えられない**
- **見込んでいる罠**: モノ入力では 2 本目のチャネルが未定義。読まずに
  1 本目を複製する（Doubler がやっている）
- **測った**: 層の群遅延は 48 kHz で **12 サンプル（4x、約 0.24 ms）**。
  `SURFACE` = 1 の層はピンクノイズ −18 dBFS に対して **−31 dBFS**（原音より
  13 dB 下）
- **決めた**: ノイズのシードは**プロセス内のインスタンス番号**から作る。
  時計は読まない（`air-core` を時計から遠ざけたまま）。同じシードの 2 本が
  ステレオペアの左右に載ると `WIDTH` が 1 段上で台無しになる

---

### AIR-5 — Follow Engine ✅

`follow.rs`。3 本の検出と、`(1 − 深さ) + 深さ × 検出値` の合成。

- **完了条件**:
  - [ ] 深さが全部 0 のとき、係数が入力によらず**厳密に 1**
  - [ ] `ENVELOPE` 深さ 1 で、無音入力に対し層が無音へ収束
  - [ ] `BRIGHTNESS` 深さ 1 で暗い素材に対し層が閉じる
  - [ ] `BRIGHTNESS` の動作量が入力ゲイン ±12 dB で 0.2 dB 以内
  - [ ] `TRANSIENT` 深さ 1 で、持続音に閉じアタックで開く
  - [ ] ノイズ層の `ENVELOPE` に下限が効いていて 0 にできない
  - [ ] 3 つの係数が個別に取り出せる（UI が使う）
  - [ ] **深さ全部 0 = Velour、`TRANSIENT` だけ 1 = Sparkleur の Sparkle**
        の形になっている（`REQ-AIR-002` の監査）
- **要件**: REQ-AIR-002, REQ-AIR-007
- **依存**: AIR-4
- **決めた**（`dsp.md`）: `TRANSIENT` は**検出帯に通してから**比を取る。
  これで Sparkleur の band 5 とほぼ同じ状況になり、`SNAP_RANGE_DB = 6.0` が
  出発点として使える。フルバンドのままだと低域の遅いエンベロープに薄められて
  ハットでも開かない。帯域通過は `BRIGHTNESS` と共用で 1 本
- **やること**: **`nxe_audio::envelope::Power` を上げる。** パワーフォロワの
  実装が既に 2 つある（`guard::Follower` と `sparkle::Power`、どちらも private）。
  Air が 3 つ目なので、architecture.md の「3 個目の客が来たら上げる」に当たる。
  **時定数は上げない** — 秒で受け取るだけにして値は呼び出し側に残す（`SPK-1`）
- **測って決めた**: `BRIGHT_FLOOR_DB` / `RANGE` = **−12 / 18 dB**（分布表は
  `dsp.md`）。ピンクが 0.65 で「開いているが上がある」。床をピンクに合わせると
  検出器が振り切って `BRIGHTNESS` が何もしないノブになる
- **測った**: 入力ゲイン ±12 dB で比が **−0.284 / −0.284 dB**（完全一致）。
  `TRANSIENT` はバースト列で 1.000、定常正弦で 0.023
- **残っている**: `ENV_FLOOR_DB` / `RANGE` = −48 / 36 dB は**耳**（`AIR-13`）
- **踏んだ罠**: **書き換えが rustfmt に流された。** 整形済みのファイルに対して
  自分が書いた文字列でマッチさせると黙って何も起きない
  （`.agents/rules/rust.md` にある形）。テストの期待値の配列で 1 回踏んだ
- **踏んだ罠**: **1e9 のサンプルは本物のイベント。** hostile テストの後、
  100 ms リリースの遅いフォロワが忘れるまで**数秒かかる**ので、3 秒の素材では
  「ゲートが閉じたまま」に見えた。バグではないので、テストの側を伸ばした
- **見込んでいる罠**: **エンベロープが絡むテストはブロックごとに
  パラメータを設定して測る。** Velour の `EMOTION` が**全部逆に測れた**
  （検波器がまだ無音しか見ていなかった）。`blocked` 相当のヘルパを先に作る

---

### AIR-6 — 保護（Excess Guard）✅

`guard.rs`。`nxe_audio::guard` の値違い。検出帯は `FOCUS` に追従。

- **完了条件**:
  - [ ] **スペクトル的に普通の素材でちょうど 0 dB**
        （ピンクノイズと定常倍音列。`SPK-18` の再発防止）
  - [ ] 動作量が入力ゲイン ±12 dB で 0.2 dB 以内
  - [ ] 偏差を最小にすると保護がちょうど無効
  - [ ] `FOCUS` を動かすと検出帯が追従する
  - [ ] 生成層のゲインだけを触り、原音に一切影響しない
- **要件**: REQ-AIR-009
- **依存**: AIR-5
- **見込んでいる罠**: **参照帯が検出帯を含む。** Sparkleur が出荷時の
  しきい値でこれをやっていて、**普通の素材から常時 1.3 dB 引いていた**
  （`SPK-18`）。`FOCUS` で両方が動く Air は踏みやすい
- **避けた**: 帯は `follow::bands_of` を共用しているので、「検出帯が `FOCUS` に
  追従する」が 2 箇所ではなく 1 箇所の事実になっている。参照帯が検出帯に
  届かないことは全レート・全 `FOCUS` でテストに固定した
- **測って置いた**: しきい値 **+9 dB**。ピンクと定常倍音列が **0.00 dB**、
  ハイパスしたピンクが −12.00 dB（分布表は `dsp.md`）
- **やった**: `nxe_audio::guard` に `retune` と `set_thresholds` を足した。
  作り直すと参照フォロワが空になり、**相対検出器は空の参照に対して無限に
  harsh に見える** ので、`FOCUS` を動かすたび層に穴が開く
- **踏んだ罠（3 回目）**: **整形済みのファイルへの文字列置換が黙って失敗する。**
  `process` の中身を丸ごと差し替えるつもりが当たらず、**保護が配線されていない
  まま「全部 0.00 dB」という good に見える測定結果**が出た。`SPK-18` の
  「効くはずのものが配線されていない」と同じ形。**置換のあとは grep する**

---

### AIR-7 — 詰め ✅

レート・ブロックサイズ・極端値・スムージング。

- **完了条件**:
  - [ ] ブロックサイズ 1 / 64 / 512 / 4096 の出力が一致
  - [ ] 44.1 / 48 / 96 / 192 kHz で係数が発散しない
  - [ ] 全パラメータの極端値で非有限値を出さず panic しない
  - [ ] 非有限のサンプル 1 個で検出器がラッチしない
  - [ ] 音声スレッドの経路に確保が無い（数えるアロケータ）
  - [ ] パラメータの段変化でジッパーが出ない（**測定が失敗できることも
        同じテストで確かめる** — `VEL-10`）
- **要件**: REQ-AIR-016, REQ-AIR-017
- **依存**: AIR-6
- **見込んでいる罠**: 検出器のラッチ。Sparkleur では**実際にラッチして
  いた**ので、両チャネルを入口で消毒する
- **踏んだ罠**: **継ぎ目の測り方を 3 回間違えた**（詳細は `dsp.md`）。
  ノイズが混ざっていると測れない → 絶対値の跳びでは測れない →
  高い層は 2 階差分でも測れない。**4 回目でようやく対照実験が差を出した**
- **測った**: 継ぎ目は原音に対して **−46 dB**、層自身のピークより 14 dB 下。
  ブロックサイズ 1 / 64 / 512 / 4096 は**ビット一致**

---

### AIR-8 — UI マクロ層 ✅

メインの 7 本。`nxe_ui::knob` と `nxe_plug_ui`。

- **完了条件**:
  - [ ] 7 本が並び、ドラッグ・Shift 微調整・ダブルクリックで既定に戻る
  - [ ] `.class("root")` が根に付いている（Sparkleur で 1 回抜けた）
  - [ ] 窓の高さが部品から計算されている（手置きの定数を持たない）
- **要件**: REQ-AIR-010, REQ-AIR-013
- **依存**: AIR-7
- **決めた**: **Advanced もここで入れた。** タブを持たない設計（`REQ-AIR-013`）
  なので Advanced は「別の画面」ではなく同じ列の下段で、マクロ 7 本だけ先に
  置いても窓の高さが 2 回動くだけになる。`AIR-10` に残るのは読み値と表示
- **決めた**: 7 本とも同じ大きさ。Sparkleur は「音を作る 5 つ」と「どれだけ
  届くかの 2 つ」で分けたが、Air は `MIX` も**足した層をどれだけ通すか**で
  あって原音を減らさない（`REQ-AIR-012`）

---

### AIR-9 — UI スペクトルの重ね描き ✅（ウィジェットまで）

**画面の主役。** 原音と生成層を重ねる。

- **完了条件**:
  - [ ] `FOCUS` で層の山が動き、`SURFACE` で高さが変わる
  - [ ] 保護が働くと山が沈む
  - [ ] `SURFACE` = 0 で生成層の描画がちょうど 0
  - [ ] 新規ウィジェットを作ったなら `examples/gallery.rs` に同じ変更で入れた
- **要件**: REQ-AIR-013, REQ-AIR-018
- **依存**: AIR-8
- **決めた**: **点のスペクトラム**を `nxe-ui` に新規ウィジェットとして起こす。
  原音は細い線、生成層は点の格子で、レベルが上がるほど点が密になる。
  `BLEND` を倍音側へ振ると点が縦に揃い、ノイズ側で散る — **2 系統の違いが
  そのまま図になる**。`FOCUS` で山が動き、`SURFACE` で高さが変わり、保護が
  働くと沈む、は全部この 1 枚で成立する
- **気をつけること**: **点の配置をフレームごとの乱数にしない。** 明滅して
  読めなくなるうえ、gallery で見ても同じ絵が 2 度出ないのでレビューできない。
  スペクトルからの決定的な関数にする。点の数は上限を決める（femtovg に
  数百個の矩形を毎フレーム投げることになる）
- **やった**: `nxe_ui::dots::DotField`（16 行 × 列は呼び出し側）と gallery の
  パネル。散らしは `offset_of(column, row)` の splitmix32 で**決定的**、
  上端のセルだけ端数ぶん小さく描くので縁が階段にならない
- **分けた**: **図を窓に載せるのは `AIR-11`。** ウィジェットは gallery で
  単体でレビューできる（規約がそれを要求している）ので、そこで 1 単位。
  窓に載せるには解析の配線が要るので、`AIR-10` の読み値と一緒にやる

---

### AIR-10 — UI 読み値と解析の配線 ✅（`AIR-11` を含む）

Follow の 3 係数、保護の引き量、相関、IN / OUT。Advanced の偏差。

- **完了条件**:
  - [ ] Follow の 3 係数が**個別に**出る（合成後の 1 本ではない）
  - [ ] 相関の表示が `WIDTH` に追従する
  - [ ] 読み値の桁数が変わっても横にずれない（固定幅 + 右寄せ）
  - [ ] **全パラメータにコントロールがある**（`#[id]` を走査するテスト）
- **要件**: REQ-AIR-010, REQ-AIR-013, REQ-AIR-018
- **依存**: AIR-9
- **見込んでいる罠**: 符号と桁数で読み値がチカチカする。`SPK-19` で
  「符号は常に出す + 固定幅の箱 + 右寄せ」に落ち着いている
- **一緒にやった**: Advanced は `AIR-8` で入れたので、ここは読み値の帯
  （7 セル）・メーター・図の配線・`analysis.rs`。**図を窓に載せるのに解析が
  要る**ので `AIR-11` と分ける意味が無かった
- **出しているもの**: `IN` / `OUT` / `GUARD`（引き量）/ `WIDTH`（層の相関）/
  `ENV` `BRT` `TRN`（Follow 3 本を**個別に**）。新しく測るものは 1 つも無い
- **踏んだ罠**: 「読み値は固定幅」をテストに書こうとして `len() == 5` を
  アサートしたら落ちた。**桁数は書式では揃わない**（`+0.0` と `-40.0`）—
  揃えているのは `readout::cell` の固定幅の箱と右寄せのほうで、書式が
  保証できるのは符号だけ
- **出荷して壊れていた**: **`process` の publish ブロックが丸ごと落ちていた。**
  整形済みファイルへの文字列置換が当たらず（今日 6 回目）、コンパイルは通り、
  テストは 461 本全部通り、**窓は「信号の来ていないトラック」と同じ絵で
  静止していた**。実機で見て初めて分かった
- **実機で 2 つ目**: **`OUTPUT` が幅ゼロに潰れ、Advanced の行が窓より広かった。**
  `knob_block` の `Stretch(1.0)` が `Auto` 幅の親の中で 0 になり、ノブと
  ラベルと数値が同じ原点から描かれた（`.agents/rules/vizia.md` にそのまま
  書いてある形）。加えて 3 列 + 側で 700 px、パネルは 628 px しか無く、
  **はみ出した `OUTPUT` がメーターの下に潜っていた**。2 列に畳んで、
  **固定幅の合計が窓に収まることをテストにした**（3 列にすると落ちる）
- **入れた**: `analysis.rs` の `Handoff` を全部 `lib.rs` の `analysis.X.write`
  と突き合わせるスキャンテスト。**書式で改行されても落ちないよう空白を
  剥がしてから探す**。1 行消して落ちることも確かめた。
  Sparkleur の「書かれているのに誰も読んでいない」（`SPK-19`）の裏返しで、
  **どちらの向きも無言**

---

### AIR-11 — 解析の配線 ✅（`AIR-10` に含めた）

`nxe_dsp::Handoff` で音声スレッドから UI へ。

- **完了条件**:
  - [ ] 音が止まると表示が減衰して消える（**3 秒以内に完全に 0**）
  - [ ] 解析を止めても音が変わらない（表示専用）
  - [ ] 音声スレッド側に確保もロックも無い
- **要件**: REQ-AIR-018
- **依存**: AIR-10

---

### AIR-12 — CPU 予算 ✅

- **完了条件**:
  - [ ] criterion で 4x / 512 サンプル / 48 kHz が **533 µs 以内**
  - [ ] 内訳を記録する（エンジン / `Spectrum` × 2 / `Level`）
- **要件**: REQ-AIR-016
- **依存**: AIR-11
- **見込み**: クロスオーバーの木が無いので Sparkleur（129 µs）より軽い。
  **`Spectrum` を 2 本持つのが一番効く**（Sparkleur は 32 バンドで 15.2 µs）
- **実測**: エンジン **47.2 µs**（予算 533 の 9 %）。ラッパの解析を足して
  80 µs 前後。見込みどおり Sparkleur より軽い
- **内訳**: 倍音層が 30 µs（4x）。`BLEND` = 1 と `SURFACE` = 0 がどちらも
  17.1 µs で一致するのは、**ゲインがちょうど 0 のとき 4x のブロックを
  回していない**から。`FOCUS` の再構築はブロックあたり 0.4 µs
- **`Spectrum` を削る必要は無かった。** 32 バンド 2 本のまま

---

### AIR-13 — 既定値と耳 ✅

- **完了条件**:
  - [ ] **測定で決められるものを先に測る**（`SPK-18` では半分がそうだった）
  - [ ] 既定値で「元音に無かった表面が足されている」が成立する
  - [ ] 挿して `SURFACE` を 1 本回すだけで判断できる
- **要件**: REQ-AIR-022
- **依存**: AIR-12
- **聴く先**: ボーカルチョップ、スネア / クラップ、パッド、
  ダブステップベース、ドラムバス（構想の用途表）
- **決まった**: `SURFACE` 0.50 / `BLEND` 0.50 / `CHARACTER` 0.72 /
  `FOCUS` −0.50 / `WIDTH` 0.60 / `FOLLOW` **1.00** / `MIX` 1.00
- **測って固定した**（`defaults.rs`）: ピンクで層 −32.4 dB・保護 0.00、
  パッドで −54.6、ハット列で −24.1・保護 −9.2。**持続音と打点で 30 dB 違う**
  のは深さ 1 の `TRANSIENT` の機構どおり
- **踏んだ罠**: **作ったばかりの `FloatParam` のスムーザは 0 から始まる。**
  ホストは `initialize` で起こすが、テストがそれをやらないと**全マクロが
  下限のプラグイン**を測ることになる。最初の測定は層 **−200 dB**（= 無音）で、
  「既定値は何も足していない」という完全に harness だけの結論が出た

---

## 見込んでいる罠（Velour と Sparkleur で払った授業料）

`docs/HANDOVER.md` に全部あるが、Air で特に効きそうなもの:

- **テスト信号の周波数を「周期数」で書かない。** `harmonics::tone` と
  `bin_of` を使う。Velour で 3 回踏んだ
- **引き算で「足した層」を取り出さない。** `出力 − 原音` は精度をほぼ捨てる。
  **Air は加算型なので層を直接取れる** — テスト用に取り出せる形で書く
- **テスト信号は自分でも測る。** `SPK-18` の DC が乗ったノイズ
- **測定が失敗できることを同じテストで確かめる。** `VEL-10`

---

### AIR-14 — 窓の追随

`SPK-23` が決めた型に合わせて組み直す。**Air には `MODE` を置かない**
（弱いと判断した 2 本に限る、`docs/implementation/roadmap.md`）。

- **完了条件**: 17 個すべてに控えがある。主図が反転面に載っている。
  Sparkleur と同じ幅・同じグリッドに乗る。`analysis` の全ハンドオフが
  publish されている既存テストが通る
- **要件**: REQ-AIR-013
- **依存**: SPK-23
