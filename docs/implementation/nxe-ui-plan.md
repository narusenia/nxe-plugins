# nxe-ui 実装計画

> 最終更新: 2026-08-31

`crates/nxe-ui` は全プラグインが共有する Vizia ウィジェットとテーマ。守るべき
規約は [`../../.agents/rules/vizia.md`](../../.agents/rules/vizia.md)、置き場所と
依存の向きは [`../specifications/architecture.md`](../specifications/architecture.md)。

状態は [`backlog.md`](backlog.md)、順序の根拠は [`roadmap.md`](roadmap.md)。

## 方針

**nih-plug を知らない。** ウィジェットは値とコールバックしか受け取らない。
この境界があるから `examples/gallery` が単体のデスクトップアプリとして起動でき、
DAW を開かずに UI を反復できる。

**gallery に無いウィジェットは作らない。** 追加は同じ変更で gallery にも並べる。
並んでいないものはレビューされない。

**widget にするのは描画かふるまいを持つものだけ。** Panel / Section / Row /
Label / Divider / Disclosure は CSS のクラスと Vizia 組み込みの `Button` で
足りる。これらを widget にすると数が増えて意味が増えない。

## コンポーネント一覧

| ウィジェット | 単位 | 誰が使うか |
|---|---|---|
| `Knob` | UI-4 | Doubler のマクロ層 |
| `Bar` | UI-5 | Doubler の Detail 表 |
| `SegmentedControl` | UI-6 | Doubler の `Voices` / `Source` |
| `PolarField` | UI-7 | Doubler の Voice Field |
| `CurveView` | UI-10 | Doubler の Filter View / Velour の伝達曲線 |
| `BandField` | UI-13 | Velour の Band Field |
| `Meter` | UI-8 | Velour の IN / OUT |
| `ToggleSwitch` | UI-9 | **まだ誰も使わない**（下記） |

`Meter` は Doubler が使わないので `UI-8` は着手していなかった。**Velour で
使う相手が現れた**（2026-08-26、`REQ-VEL-013`）。実戦で検証されないまま
gallery に並ぶ心配が消えたので、Velour の要求に合わせて作る。

`ToggleSwitch`（`UI-9`）は**2 個目のプラグインでも要らなかった**。Velour の
`SOLO` は `.segment` を当てた `Label` に `checked` と `on_press` で足りる
（`crates/nxe-ui/README.md`）。**3 個目でも要らなければ計画から落とす。**

## v0.2.0 の作り直し — 何を変えて、何を変えないか

**参照は [fors](https://fors.fm) の 3 本**（Opal / Pivot / Tela）。狙いは
「fors のように読めて、初見の人にも分かる」で、この 2 つは fors 本体では
両立していない — あちらは語を捨てて記号だけにしている。

**変えるもの 4 つ。**

- **アクセントを本ごとに 1 色**にする。明度・彩度を揃えた家族なので、5 本を
  並べても 5 色のばらけた窓にはならない（`UI-15`）
- **1 窓に 1 枚だけ面を反転**し、そこに主図を載せる。fors_tela の淡い面
  （`UI-18`）
- **専用のピクトグラムを 10〜16 個**描き起こし、**常に語と併記**する。fors の
  一番の特徴で、語を残すのが「初見で分かる」の担保（`UI-17`）
- **書体を Inter に**する。Light / Regular / Bold の 3 面（`UI-16`）

**変えないもの。** どれも `SPK-19` で払って手に入れた規則で、fors 方向と
矛盾しない。

- **角は 0。** 参照も全部そうしている
- **罫線が構造の装置。** 影も丸みも無い
- **勾配は量にだけ。** 状態はフラット
- **動く数字は mono。** Inter の tabular figures には `tnum` が要り、この
  vizia には OpenType feature に触る道が無い。桁で幅が動く
- **`nxe-ui` は nih-plug を知らない。** 記号もパレットも値とコールバックの側

**「初見で分かる」はヘッダ右の 1 行で担保する**（`UI-19`）。ホバーしている
コントロールの説明がいつも同じ場所に出る。tooltip にしないのは、平面の上に
もう 1 層出すと静けさが死ぬのと、当たり判定の罠が `vizia.md` に記録済みだから。

## 実装単位

### UI-1 — クレート骨格・テーマ・gallery ✅

`crates/nxe-ui` を作り、テーマトークンと CSS の土台を置く。トークンの一覧と値は
[`../../plugins/doubler/docs/specifications/ui.md`](../../plugins/doubler/docs/specifications/ui.md)
の「トークン」節が正（shadcn / IntentUI 系のニュートラル多段 + アクセント 1 色）。

**トークンは Rust の定数を正とし、CSS はそれを埋め込んで生成する。** カスタム
描画のウィジェットは Rust 側で色を必要とするので、CSS を正にすると二重管理に
なる。Vizia の CSS が custom property をどこまで解釈するかにも依存させない。

`examples/gallery.rs` が単体で起動し、トークンの色見本、角丸 3 段、間隔の
5 段、文字の 2 段、CSS のクラス（Panel / Section / Row / Label / Divider）を
並べる。

- **完了条件**: `mise run gallery` でウィンドウが開き、上記の見本が見える。
  色のリテラルが CSS にも `View::draw` にも直接書かれていない（すべて
  `theme` モジュール経由）。`nxe-ui` の依存に nih-plug が無い
- **依存**: INFRA-1

### UI-2 — Lucide アイコン ✅

`lucide.ttf` を埋め込んで `cx.add_font_mem()` で登録し、Lucide の
`font/info.json` から名前 → コードポイントの定数モジュールを生成する。生成物は
手で編集しない。

- **完了条件**: `nxe_ui::icon::CHEVRON_DOWN` のように参照して描ける ✅。gallery に
  使用するアイコンの一覧が並ぶ ✅。生成手順が書かれている ✅
  （`scripts/generate-icons.py` の docstring と `mise run icons:generate`）
- **依存**: UI-1
- **踏んだ罠**: **スタイルシートの `font-family` は埋め込みフォントを選ばない**
  （このピン留めされた vizia リビジョンでは）。宣言はパースされ変換コードも
  正しく見えるのに、フォールバックフェイスからグリフが来る。コードポイントが
  私用領域にあるため、**失敗が「無関係な CJK のグリフ」として描かれる**ので
  フォントファイルが壊れているように見える。`icon::label` が modifier で
  ファミリを指定する形にし、`.icon` クラスは色だけを持つ。`.icon` が
  `font-family` を含まないことをテストで固定した

### UI-3 — 共通の入力ふるまい ✅（値の直接入力を除く）

`Knob` と `Bar` が共有する操作を 1 箇所に置く。個々のウィジェットで再実装
させない。

- 縦ドラッグで変更、`Shift` で微調整
- ダブルクリックで既定値に戻す
- `Ctrl` + クリックでインラインのテキスト入力（ダブルクリックはリセットに
  使うので別のキーに割り当てる）
- ホバーでツールチップ。表示文字列は呼び出し側が渡す
- ドラッグの開始と終了をコールバックで通知する（ホストのオートメーション記録を
  1 操作にまとめるために、プラグイン側が必要とする）

- **完了条件**: 5 つのふるまいが動く（**値の直接入力以外** ✅）。ドラッグ開始／
  終了のコールバックが 1 回ずつ呼ばれる ✅
- **依存**: UI-1
- **決めたこと**:
  - **`UI-4`（Knob）と同時に実装した。** `UI-3` 単体では gallery に並べるものが
    無く、確認用のダミーウィジェットを作って後で捨てることになる。コミットは
    分けてあるので `UI-3` だけ revert できる
  - 値の直接入力は **`Cmd`（または `Ctrl`）+ クリック**。macOS では `Ctrl` +
    クリックが OS レベルで副ボタンになり、右クリックはホストのパラメータ
    メニューの領分なので
  - 値の算術（`Drag::value_after`）は純粋関数に切り出した。ここが壊れることが
    一番多いので、ウィンドウなしでテストできる形にしてある
  - **ツールチップ**（2026-08-26）: vizia の `.tooltip(…)` モディファイアが
    ビューの生成とホバーでの表示まで持っているので、`nxe-ui` がするのは
    **見た目を決めることだけ**。遅延はトランジションの中にある（500 ms）—
    ノブの列を横切るときに箱の跡が残らないように
  - **値の直接入力は入れて外した**（2026-08-26）。`ValueEntry` は `nxe-ui` に
    残っていて gallery で動くが、**プラグインのノブの下に置くと editor の表示が
    一切更新されなくなる** — 値もボイス図も、窓を開いた時点の表示のまま固まる。
    二分探索で `ValueEntry` を載せたコミットが原因と特定済み。**潰した仮説**:
    形は `nih_plug_vizia` の `ParamSlider` と同じ（`#[derive(Lens)]` + 自分の
    フィールドへの `Binding`）、panic は起きていない、外すと直る。gallery では
    再現しないので、`ParamWidgetBase` 由来のレンズとの組み合わせが疑わしい
  - 設計自体は以下のまま（戻すときの前提）: **数値ラベル自身が入り口**。
    ノブに `Cmd` + クリックする案から変えた — ノブにはテキスト欄を置く場所が
    無く、単位も解析も知らない。数値の上は人が最初に打ち込もうとする場所でもある
  - 解析は**パラメータ自身が持つ** nih-plug の `string_to_normalized_value`。
    `-3 dB` も `L70` もそれを意味するパラメータが読む。読めない文字列は
    値を変えない
  - 打ち込みも**1 組のジェスチャー**としてホストに届ける。ドラッグと同じ扱い
  - Detail 表の数値は**実効値**（マクロ × 形状）で、その積を持つものが無いので
    入力できない。編集は隣のバー
  - `Gesture::Edit` は通知として残す（ノブ側から拾いたい呼び出し側のために）

### UI-4 — Knob ✅

弧を `View::draw` で描く。大小 2 サイズ。値は正規化 0–1、表示は呼び出し側の
文字列。

- **完了条件**: gallery に大小が並び、UI-3 のふるまいが効く ✅。弧の描画が
  0 と 1 の端で破綻しない ✅
- **依存**: UI-3
- **決めたこと**:
  - **vizia の組み込み `Knob` は使わない。** `Lens` 前提で `on_changing` しか
    無く、プラグインが必要とする**ジェスチャーの開始と終了**を通知できない
    （ホストがオートメーションを 1 回の編集として記録できなくなる）。
    Shift 微調整・ダブルクリックリセット・値入力を閉じた view の外から足す道も
    無い
  - 値は `impl Res<f32>` で受ける。静的な `f32` とレンズの両方を受けられる
    vizia の仕組みなので、`nxe-ui` が nih-plug を知らないまま、プラグイン側は
    レンズを渡してオートメーション由来の更新を受け取れる
  - 弧に accent → accent-bright のグラデーションを掛けている。`ui.md` が
    「1 箇所だけ許す例外」としたところ

### UI-5 — Bar ✅

横方向の細いスライダ。塗りが値、右に呼び出し側の文字列。

- **完了条件**: gallery に並び、UI-3 のふるまいが効く（ドラッグは縦）✅
- **依存**: UI-3
- **決めたこと**:
  - **単極と双極の 2 形態を持つ。** Detail 表は `Delay_i` / `Gain_i` が単極、
    `Detune_i` / `Pan_i` が双極（中央から伸びる）。`Bar::new` と
    `Bar::bipolar` で切り替える
  - 双極のときは**中央のマークを塗りの上に描く**。バーがほぼ満杯でも「中央が
    どこか」が読めるように
  - 塗りの範囲を返す `Bar::span` を純粋関数に切り出した。双極が逆向きに伸びる
    のが一番起きやすい間違いなので、ウィンドウなしでテストしてある
  - `input.rs` をそのまま再利用できた（`UI-3` の切り出しが機能している確認）

### UI-6 — SegmentedControl ✅

ボタン列。選択中に `--accent`。2 択以上の任意個。

- **完了条件**: gallery に 2 択と 3 択が並び、クリックで選択が変わる ✅。
  キーボードの左右で移動できる ✅
- **依存**: UI-1
- **決めたこと**:
  - **`Knob` / `Bar` と違い `Lens` を要求する。** 各セグメントが「自分が選択中か」
    という反応状態を個別に持つ必要があり、それには選択値をセグメントごとに
    map しないといけない。`Res` にはその能力が無い。実際の呼び出し側（gallery の
    モデル、プラグインのパラメータ）はどちらもレンズを持っているので実害は無い
  - **`View::draw` を書かない。** 角丸の箱を並べて 1 つを強調するのは CSS の
    仕事で、スタイルシート経由にするとテーマのトランジションがそのまま効く。
    `:checked` 疑似クラスで選択中を表現している
  - ハンドラは `Arc` で共有する。vizia の `on_press` がアクションに
    `Send + Sync` を要求するため
  - **キーボード操作**（2026-08-26）: **フォーカスを持つのは行そのもの**で、
    個々のセグメントではない。1 つの選択肢なので `Tab` で 1 回止まり、
    左右で中を移動する。クリックしたときも行にフォーカスを渡すので、
    ポインタで選んだ続きをキーボードで動かせる
  - 端では**キーを consume しない**。何も起きないのに他へ渡らないのは、
    その先に別のキー操作があったときに困る
  - 現在値は**ビュー自身が持つ**（レンズからのバインドで同期）。矢印キーは
    「今どこか」を知る必要があり、`View::event` からレンズは読めない
  - フォーカスリングは `:focus-visible`。vizia は `Tab` で来たときだけ立てる
    ので、クリックした人には出ない

### UI-7 — PolarField ✅

半円の極座標フィールド。**領域知識を持たない** — 点の一覧（角度・半径・
大きさ・ラベル・有効かどうか）と、ドラッグのコールバックだけを知る。

- 半円の目盛りと外周・中心の基準点を描く
- 点をドラッグすると角度と半径を同時に通知する
- `Shift` で微調整、点のダブルクリックでリセットを通知
- ドラッグの開始と終了を通知する
- 無効な点はディム表示。ただしドラッグは許す
- 基準点（中心の ▲）は 1 個または 2 個を呼び出し側が指定する。2 個のときは
  各点がどちらの子かを線で示す（どちらの子かも呼び出し側が渡す）

- **完了条件**: gallery で 8 点を任意に動かせる ✅。基準点 1 個／2 個の両方が
  描ける ✅。点の値を外から変えると表示が追随する ✅
- **依存**: UI-3
- **決めたこと**:
  - **点にラベルを描かない。** vizia の `draw_text` は「その view 自身の
    テキスト」しか描けないので、1 つの view に 8 個の数字を別々の位置に置く道が
    無い。代わりに**ポインタが乗っている点の index を通知する**
    （`FieldGesture::Hover`）ので、呼び出し側が別の場所の対応する行を
    ハイライトできる。小さい数字 8 個より実用的
  - **`input::Drag` を再利用しない。** あちらは縦の移動を 1 つの値にする状態機械で、
    ここは点が 2 軸同時に動く。微調整の係数（`input::FINE`）だけ共有して、
    `Shift` の効き方は揃えてある
  - ドラッグは**ポインタの絶対位置ではなく掴んだ点からの移動量**で動かす。
    絶対追従だと微調整ができない
  - `FieldPoint` に vizia の `Data` を実装した（バインディングの変更検出に必要）。
    `PartialEq` があるので 1 行
  - 基準点の位置は呼び出し側が渡す。「モノなら中央 1 個、ステレオなら左右 2 個」は
    領域知識なので、ウィジェットが決めてはいけない

### UI-10 — CurveView ✅

曲線表示。**領域知識を持たない** — 折れ線の集合、帯の区間、目盛りの x 位置、
縦ドラッグ可能なハンドルしか知らない。座標はすべて正規化（`x` は 0..1、`y` は
0..1 で 0.5 が基準線）。

- 目盛り線と基準線を描く
- 折れ線を任意本数、帯をその背後に描く
- ハンドルを縦ドラッグすると値を通知する。`Shift` で微調整、ドラッグの開始と
  終了も通知する

- **完了条件**: gallery で折れ線と帯が描け、ハンドルを縦に動かせる ✅。
  値を外から変えると表示が追随する ✅
- **依存**: UI-3
- **決めたこと**:
  - **対数軸をウィジェットに持たせない。** 当初「対数の横軸と目盛り」を
    ウィジェットの仕事にしていたが、周波数 → 0..1 の写像を呼び出し側が持つ形に
    変えた。理由は 2 つ: (1) ウィジェットが完全に領域非依存になる、
    (2) **軸ラベルを呼び出し側が置ける** — vizia の `draw_text` は view 自身の
    テキストしか描けないので、ウィジェットは自分の目盛りにラベルを付けられない。
    写像を呼び出し側が持っていれば、同じ写像でラベルを絶対配置できる
  - 縦方向の値の算術は `input::Drag::value_after` をそのまま使う。ハンドルと
    ノブが同じ移動量で同じだけ動き、`Shift` の効きも揃う
  - **ハンドルの掴み判定は横方向の距離だけ**で行う。ハンドルは x 固定なので、
    y も近いことを要求すると「上端まで上げたハンドルを引き下ろせない」になる
  - `y` は 0..1 で 0.5 が基準線（`Bar::bipolar` と同じ流儀）。dB → 0..1 の
    写像は呼び出し側

### UI-11 — `PolarField` の分布オーバーレイ ✅

方向ごとの信号量を扇形で点の下に敷く（`.density(lens)`）。

- **完了条件**: 呼び出し側が渡した配列が扇形として描かれ、渡さなければ何も
  描かれない ✅
- **依存**: UI-7
- **決めたこと**: 値は**絶対尺度**（`0` が無音、`1` が振り切り）。最初は
  ウィジェット側で最大値正規化したが、**それでは音が止まっても絵が消えない** —
  全方向が同じ率で減衰するので比が変わらない。静かなトラックで見えない問題は
  呼び出し側が dB で写像して解く（`DBL-16`）
- **決めたこと**: 色は**薄い `FOREGROUND`**（ニュートラル）。**アクセントは
  「設定したもの」の色**で、設定と結果が一目で区別できないと重ねる意味が無い。
  最初は `BORDER`（中間色のグレー）で塗ったが、下に色が付いているところで
  濁った — **信号は「明るくする」層**であるべきで、低い不透明度の光なら下を
  持ち上げるだけで済む

### UI-12 — `CurveView` の解析カーブ ✅

信号のカーブを曲線の背後に塗る（`.analysis(lens)`）。

- **完了条件**: 床から塗られ、渡さなければ何も描かれない ✅
- **依存**: UI-10
- **決めたこと**: 既存の `Vec<Curve>` に足すのではなく**別の入力**にした。
  同じ配列に混ぜると「線で描く曲線」と「塗る面」を区別できず、呼び出し側が
  順番で暗黙に約束することになる
- **決めたこと**: 色は `UI-11` と同じ理由で `BORDER`
- **決めたこと**: 帯（`Span`）**の上、他の全部の下**に描く。帯の下に置くと
  埋まる — 半透明のアクセントが 8 枚重なるとほぼ不透明になる。帯の濃さも
  0.18 → 0.06 に落とした

### UI-8 — Meter ✅

レベルメーター。**Velour の IN / OUT が使う**（`REQ-VEL-013`）。

- **完了条件**: gallery で値を動かすと追随し、ホールドの印がバーより遅れて
  落ちる ✅
- **決めたこと**: **1 本 = 1 view。** Velour は IN と OUT で計 4 本並べるが、
  どれだけ離すかはレイアウトの判断で、ウィジェットが決めることではない
  （軸のラベルを描かないのと同じ理由）
- **決めたこと**: 目盛りは**位置だけ受け取り、文字は呼び出し側**。`CurveView` と
  `BandField` と揃えた
- **決めたこと**: **フルスケールでホールドの印が白くなる。赤にしない。** この
  デザインは色相を 1 つしか持たず、それは「何を設定したか」の色
  （`crates/nxe-ui/README.md`）。「振り切った」は明度で言う
- **決めたこと**: 値は**正規化**で受け取る。dB の床をどこに置くかは
  呼び出し側（`nxe_dsp::Level` が振幅を返すのと同じ判断）
- **操作を持たない。** ここには制御するものが無いので `Drag` も使わない
- **依存**: UI-1

### UI-9 — ToggleSwitch ❌ 落とした

on/off の滑るスイッチ。**3 つのプラグインがどれも要らなかった**ので、計画に
書いてあった条件どおり落とした（`SPK-19`）。`.segment` を当てた `Label` に
`checked` と `on_press` で足りている — そちらのほうが**セグメント選択と同じ
見た目**になるので、「1 つ選ぶ」と「on/off」が別々の語彙にならずに済む。

**書かれなかったウィジェットの記録として残す。** 3 製品ぶんの証拠があって
要らなかったものは、4 個目で誰かが思い付いたときに調べ直す価値がある。

### UI-13 — BandField ✅

Velour の主役の図（`plugins/velour/docs/specifications/ui.md`）。対数周波数の
パネルに、掴める帯域の区画と解析カーブ 2 本を重ねる。**領域知識を持たない** —
Hz も dB も知らず、正規化した x と 0..=1 の高さだけを受け取る。

契約（`Band` / `BandGesture` の定義）は Velour の UI 仕様が正。

- **完了条件**: gallery で 3 区画を縦にドラッグでき、下端のレールの横ドラッグが
  別のジェスチャーとして通知され、解析カーブ 2 本が別の層として重なる ✅
- **`CurveView` を拡張しないと決めた理由**: 掴める区画・2 本目のドラッグ軸・
  区画ごとのリダクション表示・カーブ 2 本を足すと `CurveView` の表面積が倍に
  なり、Doubler の Filter View がその複雑さを何の見返りも無く払う。
  **主役の図は製品ごとに違う**というのが `PolarField` と合わせて 2 回とも正しい
- **決めたこと**: 横方向は**下端の 12 px のレール**が受ける。「区画の外側」に
  すると区画が全高になった瞬間に掴めなくなり、最初の数ピクセルの向きから
  推測するのはもっと悪い
- **決めたこと**: レールは `.focus(lens)` を繋いだときだけ生きる。繋いでいない
  ときは掴めるように**見えない**（`PolarField` のアンカーと同じ扱い）
- **決めたこと**: **区画は重なってよく、重なった領域は中心が近いほうに属する。**
  並列生成器は境界を共有するので、宣言順では意味が決まらない
- **決めたこと**: **ダブルクリックは掴んでいる状態に頼らず当たり判定する。**
  掴みがまだ開いているかはイベントの順序に依存する。`Drag` が出す `Reset` は
  転送しない（`PolarField` と同じやり方）
- **決めたこと**: ドラッグ中は**ローカルの値を先に動かす**（`CurveView` と
  同じ）。`PolarField` が楽観的更新をやめたのは呼び出し側が別のパラメータと
  突き合わせてクランプするためで、ここには突き合わせる相手がいない
- **依存**: UI-1, UI-3

### UI-15 — アクセントを本ごとに渡す ✅

`ACCENT` / `ACCENT_BRIGHT` / `ACCENT_DEEP` / `ACCENT_DIM` / `ACCENT_WASH` の
定数 5 本を `Palette` にまとめ、`theme::install(cx, palette)` で受け取る。

- **完了条件**:
  - [x] gallery の PALETTES パネルに 5 色が並ぶ。**入れ子の `Palette` モデルの
        下で `Bar` を描いて**、値ではなく経路のほうを見せている
  - [x] 生成 CSS に色リテラルが無い（既存テストを 5 パレット分に広げた）
  - [x] **5 色の明度と彩度が閾値の中に収まっている**
        （`the_palettes_are_one_family`）。色相の隔たりも固定した
        （`the_palettes_are_told_apart_by_hue`、最小 45°）
- **依存**: なし
- **決めたこと**: **色相だけを本ごとに変える。** 5 色は OKLCH で明度と彩度を
  stop ごとに揃え、色相だけ回して作った。色相は Doubler 158（jade）/
  Air 232.7（sky）/ Velour 300（violet）/ Parallax 345（rose）/
  Sparkleur 35（coral）
- **決めたこと**: **Air が sky を継ぐ。** `#38BDF8` は 4 本が出荷したアクセント
  そのままで、主題が空気の 1 本に残した。**残り 3 stop は少し動いた** — 旧
  ramp は 4 stop で色相が 230 → 243 と流れていて、家族のテストは 1 パレット
  1 色相を要求する
- **決めたこと**: **`deep` の彩度だけ許容を緩めた**（0.005 → 0.02）。あの明度で
  青と緑は sRGB を先に外れるので、明るくするのではなく彩度を落として収めた
- **見つけたこと**: **`DrawContext` は `DataContext` を実装している。**
  `cx.data::<Palette>()` が木を遡るので、**ウィジェットのコンストラクタを
  1 つも変えずに**描画時のパレットに届いた。計画では「ここが単位の重さの本体」
  と見込んでいたが、**重さはほとんど無かった** — `draw` の頭に 1 行足すだけ
- **見つけたこと**: **`theme::palette` は `&impl DataContext` を取れる。**
  組み立て中（`Context`）・イベント中・描画中のどれでも同じ呼び方になる。
  gallery が組み立て中に使っている
- **踏んだ罠**: **vizia はスタイルシートを外す手段を持たない**
  （`add_stylesheet` はあるが `remove_stylesheet` が無い）。**CSS は 1 窓に
  1 パレット分しか焼けない**ので、gallery は `NXE_GALLERY_PALETTE` で起動時に
  選ぶ。自前描画のほうはモデル経由なので 5 色同時に見える — **プラグインは
  この非対称を踏まない**（1 窓 1 パレットなので）
- **踏んだ罠**: **`Element::new(cx)` の途中で `theme::palette(cx)` を呼ぶと
  借用が衝突する。** モディファイア鎖の中で `cx` を再度借りることになるので、
  値は**手前で束ねてから**渡す
- **見て直した**: **Sparkleur は色相 50 で、`deep` が茶色になっていた。**
  あの明度では暖色は全部茶の帯に落ちる。**`deep` を明るくしても茶のまま**
  （茶 → 褐色になるだけ）で、効くのは色相だけ。**35 に動かして `deep` を
  ブリックレッド**にした。25 まで下げると accent がサーモンになって Parallax の
  ローズに寄りすぎる

### UI-16 — Inter への差し替え ✅

語の面を Geist から Inter（SIL OFL 1.1）に替える。**Light / Regular / Bold の
3 面**、数字は **Geist Mono のまま**。

- **完了条件**:
  - [x] font のテストが Inter の 3 ウェイトすべてを family `Inter` として
        解決することを固定した。**ファイルの `name` 表を読む形に書き直した**
  - [x] gallery の TEXT パネルに 3 面が並ぶ
  - [x] `assets/geist/` から Sans と Bold が消え、Mono だけ残った
- **依存**: なし
- **決めたこと**: **数字は mono を続ける。** Inter は tabular figures を持って
  いるが `tnum` は OpenType feature で、この vizia に feature を立てる道が無い
- **決めたこと**: **Light の置き場は `font::display`。** 大きい文字専用で、
  ラベルの大きさには使わない — 小さい Light は静かではなく細いだけで、暗い地の
  上では上品になる前に脆くなる。**ワードマークは Bold のまま**にした（17 px の
  Light は実機で見るまで判断できない。判断は `UI-19` のヘッダの作り直しで）
- **見つけたこと**: **予想した罠は本当にあり、そして踏まずに済んだ。**
  `Inter-Light.ttf` は `name` ID 1 が `Inter Light`、ID 16 だけが `Inter`。
  **`fontdb` は ID 16 を優先して ID 1 に落ちる**ので family `Inter` の
  weight 300 に入り、modifier で届く。逆だったら `font_weight(Light)` が
  黙って Regular を描いていた
- **見つけたこと**: **既存の font テストは何も守っていなかった。** 「名前は
  ファイルの `name` ID 1 が言っているもの」とコメントに書いてあるのに、中身は
  `assert_eq!(SANS, "Geist")` — **定数どうしを比べていただけ**で、フォルダに
  どんなフォントが入っていても通る。`name` と `OS/2` を読む形に書き直した
- **払った代償**: **アセットが 255 KB → 1.24 MB になった。** Inter の static は
  1 面 412 KB（Geist の 3 倍）で、覚えのある字だけに絞れば減るが、生成手順が
  1 本増える。**要るまで絞らない**

### UI-17 — ピクトグラム

10〜16 個の専用記号。**femtovg のパスで描く**（アイコンフォントは作らない）。

- **完了条件**: gallery に全部並ぶ。**語と対で置いた例**が 1 つある。既存の
  Lucide は消さない（chevron のような汎用はまだ要る）
- **依存**: UI-15
- **決めたこと**: **フォントにしない。** 生成ツールチェーンが 1 本増えるうえ、
  Lucide で踏んだ「ストローク幅がフォントでは変えられない」（`UI-2`）が
  そのまま来る。パスならウェイトを画面の階層に合わせられる
- **決めたこと**: **記号は語を置き換えない。** 併記が「初見で分かる」の担保で、
  記号だけにすると参照元と同じ読めなさを買うことになる
- **未決**: 何を 10〜16 個にするかは `SPK-23` が型を決める段で確定する。記号は
  パラメータの数だけ要るのではなく、**領域と操作の種類**に対して要る

### UI-18 — 反転面 ✅

地がアクセント・描画が `BACKGROUND` の面を 1 つ足す。

- **完了条件**:
  - [x] gallery の INVERTED SURFACE パネルに例が並ぶ。**黒地の上に置いて**、
        2 つの地が 1 つの窓に見えるかを見られるようにした
  - [x] **前景と背景のコントラスト比をテストが固定した**
        （`text_survives_both_grounds`、`the_inverted_surface_is_the_accent`）
- **依存**: UI-15
- **決めたこと**: **1 窓に 1 枚だけ。** 2 枚目が欲しくなったら「主題が 2 つ
  ある」ということなので、レイアウトのほうを疑う
- **決めたこと**: **反転は 3 つ目の明度ではなく、地とインクの入れ替え。**
  「面は 2 段階」の規則は生きている
- **決めたこと**: **反転を palette の変換にした**（`Palette::inverted`）。
  `surface::inverted` が部分木にモデルを建てるだけで、**ウィジェットは反転面の
  存在を知らない**。第 2 の概念（「今どっちの地に居るか」）を widget に配らずに
  済んだ
- **決めたこと**: **中性色を `Palette` の役割に入れた**（`ground` / `track` /
  `line` / `ink` / `muted` / `subtle`）。アクセントだけ差し替えても、地・罫線・
  トラックが黒のままでは図が読めない。定数のままだと**反転面の中で黙って
  間違う**
- **決めたこと**: **反転面での ramp はアルファで走る。** あの地の上でアクセント
  には行き場が無い（地がアクセントそのもの）ので、暗さで言う。`wash` は元の
  アクセント＝地そのものにしたので、「淡いほうが遠い」は塗りが面に溶けることで
  保たれる
- **踏んだ罠**: **コントラストの計算で、合成をガンマの後にやっていた。**
  femtovg は符号化された画素を合成するので、明るい地の上の黒 72 % は
  **5.6:1 であって 2.8:1 ではない**。輝度どうしを混ぜる書き方だと、実際には
  完璧に読める面でテストが落ちる
- **踏んだ罠**: **生成する CSS の中にコメントを書くと `"` で Rust の文字列
  リテラルが閉じる。** スタイルシートは 1 個の `format!` なので、中の引用符は
  使えない
- **測った値**: 反転面の `ink` は 5 パレットとも **8.5:1 以上**（AAA）、
  `muted` は 5.2:1 以上（AA）。`subtle` は 3.0:1 で、**意図的に文字の線より下** —
  罫線・単位・不活性な行のための役割で、読ませるものではない

**スクリーンショットを撮って直したもの**（テストは 1 本も落ちていない）:

- **塗りの遠い端を地そのものにしていた。** 「淡いほうが遠い」を素直に取って
  黒 → 地のグラデーションにしたところ、**バーとメーターの右半分が面に消えた**。
  終わりが見えないバーは量を測っていない。淡い端を 45 % で止めた
- **`.ink` がスタイルシートの途中にあって `.eyebrow` に負けていた。**
  どちらも単一クラスのセレクタなので**後に書いたほうが勝つ**。アクセントの上に
  灰色の eyebrow が乗っていた。ink 系はファイルの最後に置いた
- **gallery のバーに高さを与えていなかった。** 髪の毛のような線になり、
  **パレットが壊れているように見えた**（壊れていたのは gallery のレイアウト）。
  `BARS` パネルと同じ 10 px にした
- **gallery の PALETTES が横に入りきっていなかった。** 5 パレット × 4 stop に
  ラベルを付けていたので語が重なっていた。ラベルを落として stop を伸縮に

### UI-19 — ヘッダの拡張

左にワードマークと `MODE` の枠、右に**ホバー中のコントロールの 1 行説明**。

- **完了条件**: gallery でホバーすると右の文字が変わり、外れると窓の role に
  戻る。`header::HEIGHT` は変わらない（窓の高さは足し算）
- **依存**: UI-16
- **決めたこと**: **説明文字列は呼び出し側が渡す。** `nxe-ui` はパラメータを
  知らないので、ウィジェットは「ホバーした」ことだけを通知する
- **決めたこと**: **`MODE` の枠は持たない本では空にする。** 5 本でヘッダの
  高さと左端が揃っていることのほうが、枠が 1 つ空いていることより効く
- **見込んでいる罠**: ホバーの通知が**外れたときにも**来ないと、説明が最後に
  触ったもので固まる。`PolarField` の `Hover` と同じ形

### UI-20 — 幅と、窓の高さの計算

共通の幅を 720 から一段広げ（840〜900 から始める）、**窓の高さを部品の合計から
計算する形にする**。

- **完了条件**: 5 本が同じ幅に乗る。`WIDTH` / `HEIGHT` が手置きの定数ではなく
  部品の合計になっている。実機で 4 ホスト見る
- **依存**: UI-18, UI-19
- **決めたこと**: **数字は実機で settle する。** ここに書く 840〜900 は初期値で、
  正しいことを期待していない（Velour 580 → 528、Sparkleur の図 236 → 176 → 200）
- **`SPK-19` の持ち越しをここで閉じる**: 「窓の高さが手置きの定数のままで、
  中身を 1 行足すたびに切れる」
